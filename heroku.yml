setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    app: Dockerfile
  config:
    POSTGRES_HOST: DATABASE
run:
  app: FLASK_ENV=production flask run --host=0.0.0.0:$PORT
